{
  "name": "kiosk",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/kiosk",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/kiosk",
        "outputPath": "dist/apps/kiosk"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/kiosk"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "kiosk:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "kiosk:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "kiosk:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "kiosk:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/kiosk/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/kiosk/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
